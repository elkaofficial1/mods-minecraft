# 🚀 Авто-установка сборки 

Этот скрипт позволяет автоматически установить моды одним кликом. Вам не нужно вручную скачивать архивы и удалять старые файлы.

## 📂 Куда класть файл обновления?

Файл (`update.bat` или `update.sh`) должен находиться в **корневой папке** вашего Minecraft клиента.
Это папка, где лежат папки `mods`, `config`, `saves`, `screenshots`.

## 🖥️ Инструкция для Windows

1. Скачайте файл `script.bat` из этого репозитория.
2. Поместите его в папку с игрой (см. пункт выше).
3. **ОБЯЗАТЕЛЬНО ЗАКРОЙТЕ MINECRAFT И ЛАУНЧЕР!** (Иначе скрипт не сможет заменить файлы).
4. Запустите `script.bat` двойным кликом.
5. Дождитесь надписи `[SUCCESS] Update finished!` и нажмите любую клавишу.

> ⚠️ **Если Windows ругается:**
> Если появится синее окно "Windows защитил ваш компьютер" (SmartScreen):
> Нажмите **«Подробнее» (More info)** -> **«Выполнить в любом случае» (Run anyway)**.
> Это происходит, потому что бат-файлы, скачанные из интернета, считаются подозрительными. Код открыт, вы можете проверить его через Блокнот.

---

## 🐧 Инструкция для Linux

1. Скачайте файл `script.sh`.
2. Поместите его в папку с игрой.
3. Откройте терминал в этой папке.
4. Выдайте права на запуск (один раз):
   ```bash
   chmod +x update.sh
   ```

## Запустите скрипт:
   ```bash
   ./update.sh
   ```
